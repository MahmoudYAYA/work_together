{% extends 'base.html.twig' %}

{% block title %}Modifier offre{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Modifier : {{ offre.nom }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary mt-3">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('app_offre_index') }}" class="btn btn-secondary mt-3">Retour</a>
</div>
{% endblock %}
